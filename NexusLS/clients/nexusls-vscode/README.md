# Nexus Pascal VS Code Client

This is the VS Code client for NexusLS. It starts the NexusLS executable,
passes Nexus-specific initialization options, and exposes command-palette
bridges for server-side Pascal commands.

Formatting and inactive-region decorations are intentionally not ported from
the legacy Pascal Language Server client yet.

